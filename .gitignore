node_modules/
.env
.env.*
!.env.example
!.env.*.example

# sms-gateway lives on its own branch (feature/custom-sms-gateway).
# Master never tracks it; integration is HTTP-only via SMS_GATEWAY_URL env.
sms-gateway/
.DS_Store
build/
coverage/
.dart_tool/
.packages
pubspec.lock
**/build/
**/.flutter-plugins
**/.flutter-plugins-dependencies
**/.metadata
**/.symlinks/
**/Pods/
**/DerivedData/

# Secrets — never commit
.env
.env.local
.env.*.local
.env.worker
.env.admin
.env.node
worker_website/backend/.env
admin_portal/backend/.env

# Backend artefacts
**/__pycache__/
**/*.pyc
**/.venv/
**/venv/
worker_website/backend/data/*.db*
worker_website/backend/uploads/

# Frontend build artifacts (rebuilt inside Docker)
**/dist/

# Docker / Caddy local data
caddy_data/
caddy_config/
postgres_data/

# local API keys / tokens (never commit)
neon-apikey.md
.neon_api_key
*.cf_api_token
cf-apikey.md
*api-key*
*apikey*
